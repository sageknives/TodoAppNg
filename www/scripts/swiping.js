$(function() {

	/*var contentItem = document.getElementById('content');
	var mainHeaderItem = document.getElementById('header');
	var contentHammer = new Hammer(contentItem);
	var mainHeaderHammer = new Hammer(mainHeaderItem);
	contentHammer.on("swiperight", function(ev) {
	    document.getElementById('content').click();
	});
	contentHammer.on("swipeleft", function(ev) {
	    $('#log-button').click();
	});
	mainHeaderHammer.on("swipeleft", function(ev) {
	    if(mainHeaderItem.hasClass('todo-open')){
			$('#todo-button').click();
		}
	});
	
	mainHeaderHammer.on("swiperight", function(ev) {
    	if($('#main-header').hasClass('log-open')){
			$('#log-button').click();
		}
		else if($('#main-header').hasClass('todo-open')){
			$(".todolist").each(function(){
				if($(this).hasClass('active')){
					var ulItemId = $(this).attr('id');
					$('#' + ulItemId + ' h4').each(function(){
						$(this).click();
					});
				}
			});
		}
	});*/
}());